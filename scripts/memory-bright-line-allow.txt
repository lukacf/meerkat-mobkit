# Allowlist for scripts/check-memory-bright-line (the §12 memory bright-line
# ratchet — see docs/design/agent-memory-architecture.md §12).
#
# Entry formats (one per line, justification comment REQUIRED on the line):
#
#   <crate-name>                       # why this dependency is not retrieval-index/embedding machinery
#   src:<repo-relative-path>:<pattern-name>  # why this source hit is a false positive
#
# Pattern names: fts, virtual-table, sql-match, embedding, vector_, hnsw,
# meerkat-memory
#
# Keep this file limited to the pinned §8.4 exception below and genuine false
# positives. Adding an entry to smuggle in actual retrieval-index or embedding
# machinery defeats the ratchet — that work belongs in the memory hub
# (docs/design/memory-hub-roadmap.md).
#
# The §8.4 exception, pinned: the Distiller's post-compaction harvest reads
# meerkat's OWN session semantic store (MemoryStore::search over the discard
# range) READ-ONLY. This reads meerkat's store; the §12 bright line governs
# the bundled agent-memory store, which stays plain B-tree SQLite. Entries
# suppress per (file, pattern): meerkat-memory/HNSW usage from any OTHER
# memory-module file (coordinator.rs, selector.rs, …) still fails the gate.

meerkat-memory  # §8.4 read-only host-side harvest of meerkat's OWN session semantic store; the bundled agent-memory store stays plain B-tree SQLite
src:meerkat-mobkit/src/memory/distiller.rs:hnsw  # §8.4 HnswDiscardSource: read-only compaction-discard harvest over meerkat's own store
src:meerkat-mobkit/src/memory/distiller.rs:meerkat-memory  # §8.4 harvest import site (meerkat_memory::HnswMemoryStore::open, one query, read-only)
src:meerkat-mobkit/src/memory/mod.rs:hnsw  # re-export of the §8.4 HnswDiscardSource only
src:meerkat-mobkit/src/memory_wiring.rs:hnsw  # §8.4 wiring only: constructs the read-only HnswDiscardSource for the Distiller (same pinned exception as distiller.rs)
