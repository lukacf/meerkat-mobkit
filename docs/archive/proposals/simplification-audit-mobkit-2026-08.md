<!-- Produced 2026-08-07 by the mobkit-lane simplification audit (42 agents: 6 subsystem
     readers, 35 adversarial challenges, 1 synthesis) against wave-0.8.15 @ dae52128,
     under the owner simplification directive. Companion to seam-assessment-mobkit.md. -->

# MobKit Simplification Audit - Wave011 Synthesis for the 0.8.16 Queue

Scope: 47 capability judgments across memory, schedule, continuity-heal, projections, orchestration-extras, and surfaces; every cut/meta-fix candidate was adversarially re-verified against the tree, both fleets (HomeCore gateway-mode, OB3 library-mode), and the incident history. 25 proposals survive, 10 are rejected as stated (most with a narrower salvage), 29 capabilities are keeps. The directive's diagnosis is confirmed empirically: no incident in the known history was ever prevented by a duplicated authority, and at least six (K1/K2, Bug C, 0.7.x gateway init, recall shadow, heal/re-Break, the resumed/created lie) were caused by one.

## 1. Surviving cut candidates and meta-fixes, ranked by simplification value

| # | Capability | Kind | One-line evidence | When | Downstream ask |
|---|---|---|---|---|---|
| 1 | workgraph_admission (774 lines + sidecar lock) | meta-fix, now a cut | Upstream store-owned refusal + newest-wins degrade already shipped at the meerkat 0.8.20 pin; mobkit's own test executes the invariant guard-less - the bricked-member failure mode no longer exists | Now | OB3: bundled SQLite or injected BigQuery workgraph store, and do writers ever mix identity/session spellings for one member's attention? |
| 2 | Console HTTP dispatcher duplicating the stdio method set | meta-fix | ~45 methods hand-copied across two ~3,200-line dispatchers plus two fail-open hand-maintained tables; the shared rpc/ module pattern already works for 4 domains | 0.8.16, per-domain | All operators: which methods/fields per transport (pin golden envelopes); HomeCore: drop the SDK HTTP fork once list_identities rides stdio? |
| 3 | Two gateway binaries with duplicated composition roots | meta-fix | K1/K2 field failures came from divergent hand wiring of the same runtime; drift is live (mobkit_gateway has no memory engines, callback bridge, or runnable host) | 0.8.16 start | OB3: confirm library mode must keep wiring NO firing host; HomeCore: binary-name pins and migrate/prune verb compatibility? |
| 4 | Dual embodiment doors (restore_flow vs materialize_inner, ~1,560 dup lines) | meta-fix | Four verified drifts, two actively harming HomeCore today: a deterministic host rejection bricks the whole bootstrap instead of parking, and bootstrap resume-fallbacks silently skip the memory harvest | 0.8.16 | HomeCore: accept 16/17 park-and-continue bootstrap and bootstrap-time harvest, or does startup automation read the all-or-nothing Result? |
| 5 | cross_mob remote control plane (~4,300 lines, keep wire_local/unwire_local/peer_info) | cut | The control listener is never bound anywhere, so every Remote dispatch must fail; the promised round-trip test does not exist; docs still say "Phase 1" three trains later | Now | Both fleets: any contacts.toml, cross_mob/* callers, or peer-key-file dependence; multi-process mob split planned within two trains? |
| 6 | Hygienist engine (~2,600 lines incl. gateway wiring, eval binary, prompt) | cut (park, not delete) | Default-off, never once run in any field deployment, unreachable from the builder, and applies through the transcript-rewrite path implicated in the 0.8.11 refusal incident; the correct-level successor (CompactionCurator seam) shipped upstream in 0.7.12 | Now | HomeCore: when transcripts grow again, is the accepted mitigation the 0.8.21 context-budget carrier + compaction, with curation returning (if ever) via the meerkat curator seam? |
| 7 | Static cron oracle + module-plane scheduling vocabulary + wire-rewrite config injection (paired cuts, ~1,100 lines engine + RPC + SDK discovery) | cut | Phase A shipped accept-and-ignore. Phase B removes the Rust engine/types, both RPC methods, SDK discovery/client surfaces, and rejects `scheduling_files`; durable `ScheduleService` remains the only authority. | 0.8.16 phase A; 0.8.22 phase B | Downstreams must remove static builder/config calls and author durable schedules. |
| 8 | Single-shot gateway mode + module-only dispatcher + SDK one-shot transports (~850-line third dispatcher copy) | cut | The production binary's default no-arg mode answers one env-var request against a hardcoded demo shell script; HomeCore provably uses only --persistent | Now (bare invocation becomes a fast usage error, never a silent default) | OB3: any harness spawning the gateway without --persistent or using the one-shot SDK client constructors? |
| 9 | Identity-to-live-member control-target resolution, twice (~1,840 lines) | meta-fix | Twin resolvers with different truth sources mean stdio retire and console retire can select different member sets for the same identity today; dead arms and a dead helper verified | 0.8.16 | HomeCore: any automation string-matching retire/respawn error payloads; OB3: confirm library mode never traverses the dispatchers. |
| 10 | RestoreSnapshotPolicy + restore_flow_for_bootstrap twin + dead RestoreOutcome payloads | cut | No production consumer reads the snapshot payload, the bundled bridge declares it unneeded, yet console add-member and reconcile still pay 14-94MB / 60-180s blob reads per Ready member (Class 2 evidence) | Now | OB3: does any code destructure RestoreOutcome fields or rely on the requires_resume_snapshot()=true default on a custom bridge? |
| 11 | LLM Selector stage (1,209 lines + profiles + env-var authority + process-global) | cut, conditioned | Two config authorities never exercised by anyone, full-sweep ceilings two orders beyond any fleet, default-flip deferred to a scorecard that never happened; condition: re-home FactorySelectorHandle (used by the LIVE distiller/steward) and the fetch/provenance types first | Now, with re-homing | OB3: any selector symbol references (compile-loud on repin); HomeCore: confirm "selector installed" never logged. |
| 12 | Console identity read-model self-heal (read path writing authority) | meta-fix | The write-path reconcile has existed since the same commit that added the console copy; the read-path heal can Suspend/re-lease/fence-advance - a console READ can Break an identity under contention | 0.8.16 (route console sends through identity delivery so first send still heals) | Both fleets: grep logs - has the console-side heal line ever fired, and what rotated a session outside the identity bridge? |
| 13 | rt:{identity}:{generation} parsing scattered across six divergent parsers | meta-fix | Two byte-identical duplicates, edge-disagreeing validators, decode-first left to caller convention; all minted generations are canonical u64, so consolidation into member_comms_id is behavior-preserving | Now | Both fleets: any hand-written rt:* alias with a non-canonical generation segment in stored rosters or client code? |
| 14 | Operator scope (provisional keying, resolver, steward routing) | cut | Complete and activatable but zero activators; console-principal keying would misattribute HomeCore's Telegram/schedule turns, so any real consumer re-decides §16 Q1 anyway; preserve TrustTier::Operator, the re-derivation tags, and the scope schema variant | Now (fail loud on provisional) | HomeCore: per-operator memory within 2 releases, and would console-principal keying ever be acceptable? |
| 15 | Markdown store as a live provider | cut | Eight fail-loud engine guards and counting for a recall-only legacy path; migration fully served by the one-shot sqlite import; retire gateway string, builder method, and TS type together, keep the parser for import | Now (phase 1: fail with migration pointer) | OB3: which memory builder seam do you use; HomeCore: confirm no store override and no live .md files. |
| 16 | Classic-mob memory path (MemorySpawnCustomizer, 426 lines) | cut | The gateway has refused memory-without-roster since 2026-07-01, so the path is unreachable from every production surface; must add the matching typed builder error and migrate the shipped example | Now | Both fleets: any roster-less build enabling agent memory in staging/eval harnesses driving the builder directly? |
| 17 | Aggregator-only console dispatcher + 3 runtime-less router constructors | cut | The runtime-is-None branch is unreachable in every production composition; the constructors' only caller is one ignored test; stale docs already describe signatures that no longer exist | Now | OB3: do you ever mount a console router without a MobRuntime attached? |
| 18 | Hand-written console reducer "state machines" (state.rs) | cut | Statically proven generation theater: effects never read, results discarded, error variant runtime-unreachable; also rework the fourth consumer at the reserve_interaction seam and its lying Result | Now | OB3: any imports of the nine re-exported types or an exhaustive ConsoleSendError match with a State arm? |
| 19 | compute_reconcile_actions / ReconcileAction | cut | Zero production references; the test suite pins the dead twin while production runs a live implementation that behaves differently - drift by construction | Now (port tests to observable behavior first) | OB3: any dry-run roster-diff use of this API before a repin? |
| 20 | materialization_locks keyed lock table | cut | Statically provable redundant: strictly nested under the same identity's lifecycle lock at its only consumer; can never exclude anything | Now | Pro-forma: does anyone vendor/patch runtime.rs rather than consume the pinned crate? |
| 21 | Dead attach/spawn wrapper ladder | cut | spawn_schedule_host has zero call sites anywhere including tests; the two attach shims serve only each other and two unit tests | Now | OB3: grep for the three symbols before the repin. |
| 22 | FiringHostGatedScheduleTools (Bug C write gate) | meta-fix, KEEP until trigger | Right guard, wrong layer: a process-local bool at one ingress that would not have caught the original dual-store Bug C; composition writes bypass it inside its own boot window | Gated: store-level executor lease (NOT in the ratified 0.8.21 batch - must be added to the asks); close the steward-dream write-before-bind hole meanwhile | OB3: would your BigQuery drainer register a store-level lease, or do you require an externally-drained declaration? |
| 23 | InternalDeliveryScheduleMobHost | meta-fix, KEEP until absorbed | Upstream still routes member_send through the external door at 0.8.20 and on main; deleting it today stops every internal-only HomeCore member receiving its own schedules | Gated: upstream WorkOrigin ruling classified by authoring provenance, not target address | HomeCore: can member-targeted schedules be authored from outside the mob, and do any use identity-prompt shapes with skill_refs? |
| 24 | Per-session write-epoch witness + read absorber | meta-fix, delete WITH the crossing | Single-writer soundness is a doc comment that was already violated once in production; the store-owned head revision is the authority-owned replacement | Gated: HeadCanonical crossing; must land atomically with the console-gate swap and a re-red-verified idle-CPU gate | OB3: can your injected store serve the sub-document head read at acceptable BigQuery cost, or keep the absorber re-keyed on the store revision? |
| 25 | Capability-stack assembly convergence (memory_wiring as sole authority) | already-consolidating, finish | Two assembly authorities for one stack; every gateway-only feature that survives this audit moves into MemoryStackSeams, everything cut is deleted instead of ported | 0.8.16, sequenced after rows 6/11/14/15 | None - internal. |

## 2. Rejected candidates (do not relitigate)

| Capability | Refutation one-liner | Salvage |
|---|---|---|
| Mobpack authoring family wholesale cut | False premise: OB3 production deliberately mounts the flow-editor router ("Deliberately kept mounted", ob3_validator console.rs:141-147), and the proposed stdio unmount targets the wrong mount for that path | Unmount the stdio dispatch tables and delete the module-mode deploy advertisement (authority contradiction) now; wholesale cut gated on OB3 access logs |
| Boot-time target repair pass | Pinned meerkat 0.8.20's default resolver still persists resumable_session targets, so the bundled authoring-rewrite deletion is load-bearing today; "legacy rows long rewritten" is unmeasured; the reset-reseed lane makes unconverted rows rot silently | Delete only the boot pass after HomeCore's store query shows zero session-shaped rows; fold the check into doctor |
| Schedule claim watchdog full deletion | Step 3 (due work exists, nothing claims it) is structurally unreplaceable by TransitionCommit carriers - a stall is the absence of commits - and upstream already surfaces tick errors at 0.8.20, so half the premise is stale | Split: raw-SQL triage + steps 1-2 die on 0.8.21 list-seam faults; step 3 survives until a resident liveness verdict ships |
| dispatch_taint end-state collapse | The decorator's transcript rescan is the only resume-time taint reconstruction (the tracker is in-memory and "unrecoverable"); deleting it opens a clean-write window after every gateway restart | Carrier replaces only the live race; durable taint persistence must land first; observer trimming limited to the duplicated tool-marking path |
| Resolve-time Broken ladder (bundled) | The wire path CAN mint Broken (the real mapping deserializes the full enum; the Python SDK documents state='broken'); deleting the closed-enum kinds turns one legacy string into a fleet-wide batch deserialization failure | Delete only the SnapshotMissing softening + its overlay guard, unify one Broken arm, keep corruption kinds plus a tolerant unknown-kind fallback; gated on OB3's answer |
| N=3 byte-identical-retry park | Reachable in both modes (HomeCore runs the default eager; the lazy Broken fallthrough is comparable), 65452e85 introduced it as part of the very fix claimed to obsolete it, and it is the only catch-all for deterministic walls the typed parks cannot classify | Replace the string oracle with 0.8.21 typed terminal/retryable only after proving injected-store failures arrive typed |
| HostRejectedBuildPark string-probe deletion | The channel middle is upstream-owned and stringifies (builder errors map to Internal + to_string; MemberBuildRejection is a closed enum with no host-callback variant), so no typed variant can flow at this pin; both-doors parking converts transient startup errors into stuck-until-human | Centralize the probe behind one classifier minting a local typed BridgeError, semantics byte-identical; broadening gated on 0.8.21 carrier + HomeCore log evidence |
| WholeBlob projection + tear-repair cut (as sequenced) | Reachability is composition-conditional (InMemoryRuntimeStore hard-declares WholeBlobV1; OB3's shape unmeasured), the bundle sweeps in the fleet-saving mint/reseed, and the irreversible head-canonical pin needs a clean convergence boot first | Cross first behind convergence-then-convert, prove a reseed successor, delete one release later; never port the admissions forward |
| Freshness probe | Field-hit twice (changelog: operators restoring runtime stores from backups hit this path) and no mobkit composition constructs head-canonical yet, so "dead code behind the profile gate" is vacuous; cutting risks stale authority projecting backwards over the newer durable row - durable data loss | Bounded fix for the 82MB byte-compare (persisted digest); dies with the lane after the crossing |
| Console backfill fingerprint meta-fix (both variants) | (session_id, offset) is a position, not an identity - compaction/rewrite/fork shift offsets exactly when 0.8.21 compaction goes live on the pathological sessions; materialize-on-read breaks the stable-cursor contract and entangles the reset-reseed lane | Fix the append_source_gap unbounded-growth twin now; add interaction_id-first O(1) counterpart lookup with fingerprints demoted to legacy fallback; file the durable message-identity carrier ask |

## 3. Keep list

- Distiller: production writing half at HomeCore since 07-30; watch the HnswDiscardSource directory convention for a typed carrier; add the effort/output-token config field.
- Steward: production quarantine reviewer since 08-03; containment is structural; operator branches die with operator scope; escalation folds into the schedule carrier.
- Taint firewall: production prompt-injection defense; activation-15 proved the mechanism worked and policy, not code, was tuned.
- v2 core surface (recorder/recall/supersede/manifest/propose): HomeCore's daily path; close the builder mob-resolver gap by threading the existing seam.
- SQLite store + staged validator + secret hygiene: single write chokepoint, ratcheted bright line; what finished looks like here.
- Console Memory panel + timeline events: the operator half of the promotion loop; ship or delete placeholder tiles 8/10.
- Background budget guards: bounds a runaway engine loop to 12 runs/hour/realm against a paid API.
- Host-runnable registry + CallbackHostRunnable: HomeCore extensions' only non-LLM schedule target; survives consolidation as the executor.
- Steward dream as durable schedule: misfire-aware cadence; the boot-window dead zone dissolves with the store-level liveness ruling.
- Flow relay surface: ~80-line relay, single upstream authority; the only non-Rust path to the flow engine.
- workgraph_wiring: OB3 production; scope pinning is the correct-layer fix; finish migrating composition off the error-swallowing Option variant.
- Terminal heal verdict + heal seam: the 2026-07-29 infinite-loop fix; collapse the five respect-the-park sites into one transition function.
- Broken-identity repair supervisor: the only automatic reconcile driver for Broken identities; repair-of-one-is-reconcile-of-all is a wart, not a cut.
- Orphan unactivated-lease park: single-embodiment contract for injected lease providers - exactly the OB3 deployment class; ~170 cheap lines.
- Auxiliary backoff loops: disjoint lanes and timescales, honestly labeled.
- MOBKIT_IDENTITY_RESTORE_CONCURRENCY: shipped Bug I mitigation; retires when upstream ask 31 lands, not before.
- Roster authority guards: the consolidated survivor; both flows converge on one helper with one lock order.
- Every-boot runtime-authority mint / reset-reseed: saved the fleet twice; the crossing must carry an explicit reseed equivalent before this dies.
- HTTP console surface: HomeCore's and tux's product surface; projection sprawl collapses for free with dispatcher unification.
- Python SDK ASGI facade + SSE proxy: the only remote-reachable skin; later proxy /rpc to the console route and delete the Python JWT verifier.
- Timeline query visibility scan + anchor merge: product surface; altitude cleanup (persist the visibility verdict at append, push filtering into indexed SQL).
- SqliteConsoleLogStore: single authority, ledgered schema, scale-tested; nothing to do.
- member_comms_id codec (already-consolidating): finished module; its residual debt is survivor #13.
- ArchivedNotRevivable park (already-consolidating): load-bearing since the OB3 heal loop; string probes and the stale 0.8.15 operator text die at 0.8.21.
- Lifecycle error-string classifiers (already-consolidating): the tolerances stay, the four classifiers and both duplicate wrappers die on 0.8.21 typed disposal/archive verdicts.
- Archived-terminal overlay (already-consolidating): dies when the resume-load seam carries the store-owned terminal.
- Shared surface wiring modules (already-consolidating): the existence proof that the meta-fixes are the codebase's own pattern; continue into the oldest domains.
- MobIdentityScheduleToolDispatcher (already-consolidating, AMENDED): the boot-repair review found upstream 0.8.20 does NOT persist identity targets at authoring, so the rewrite dispatcher stays load-bearing until an identity-persisting resolver ships; fix its dispatch_with_context context drop meanwhile.

## 4. Layers we ourselves added this wave that must carry deletion triggers

Checked git log on the wave011 tree; the post-0.8.14 commits (2026-08-07, the 0.8.15 wave) add four new layers. Per the directive, each ships with an explicit deletion trigger or it becomes next quarter's feature theatre:

- f1c0387a (Bug C firing-intent write gate): self-declares stopgap, but its named trigger ("0.8.21-pair work") is wrong as written - the ratified 0.8.21 carrier batch contains no store-level write-admission lease. Action: add the executor-lease seam to the 0.8.21 ask list explicitly, and close the steward-dream write-before-bind bypass the gate itself cannot see. Trigger: lease seam lands, gate deletes.
- 4adeb258 (shadowed-tool composition WARN): deliberately warn-first because HomeCore's transitional 'memory' shadow tool rides the seam today. This is the exact warn-piling shape the directive names; without a trigger it fossilizes. Trigger: the moment HomeCore deletes its shadow tool, the warn escalates to typed refusal - put a dated entry in the 0.8.16 queue, not a comment.
- f4e0e275 + 4f0046d7 (mobkit-repair + load-bearing post-apply stderr reminder): a human-loudness layer compensating for a manual scratch-store-drop step, over a duplicate-System-rows problem whose root cause the mint-vs-resume carrier (3b08c207/75d89e60) already fixed at the source. Trigger: once the fleet is past the carrier, mobkit-repair degrades to a one-shot migration and the reminder deletes with the manual step at the HeadCanonical crossing. Related smell from the same window: the examples System-dedup helper took five same-day commits redefining its dedup key (15bc971b through c6c1f1d3) - the edge-case-by-edge-case signature inside our own wave; it dies on the same trigger.
- dae52128 (Python SDK list_identities over /console/rpc): institutionalizes the second transport that dispatcher unification (survivor #2) exists to remove. Trigger: when list_identities rides stdio via the shared method module, the SDK HTTP fork deletes in the same release.

## 5. The three questions to ask first

1. To OB3 (gates the largest single cut, 35k-line mobpack.rs): does production traffic ever hit /flow-editor or POST /flow-editor/rpc (check GKE ingress logs), and may the next mobkit pin feature-gate that mount out of the reference router - or is the authoring IDE a surface you need kept?
2. To OB3 (sequences the entire projections lane and the Broken-ladder salvage in one answer): which RuntimeStore does your external driver compose - ephemeral, durable WholeBlobV1, or custom - i.e. do committed boundaries reach BigQuery via mobkit's projection and do restarts depend on the every-boot mint; and does your injected continuity store ever mint Broken from resolve_many, with which failure kinds?
3. To HomeCore (unblocks the boot-repair deletion, the authoring-rewrite endgame, and the watchdog split): run the schedule-store census (any resumable_session-shaped target rows; gateway journal grep for repaired>0), confirm whether a runtime.sqlite reseed preserves the session-to-member binding that delivery-time recovery needs, and confirm whether the 0.8.21 pairing includes an identity-persisting authoring resolver plus a resident schedule-liveness verdict.

Runner-up asks worth batching into the same round trips: HomeCore's acceptance of the irreversible head-canonical pin (one clean convergence boot, reseed equivalent proven first), the three log greps (N=3 park firings, host-rejected transient replies, console self-heal line), and HomeCore's per-operator-memory intent within two releases.


## CORRECTION 2026-08-07 (owner evidence): cross_mob remote control plane cut is REFUTED

Survivor #5 (cut the ~4,300-line cross_mob remote control plane) is WITHDRAWN and moves
to the rejected table. The audit's survey universe was the two known fleets (HomeCore
gateway-mode, OB3 library-mode) plus the mobkit tree's own compositions; the owner
identified a third production consumer the audit never surveyed:
`~/src/remotabot/meerkat-app`. Verified in that tree: the desktop gateway routes the FULL
cross_mob method set including the remote verbs (`mobkit/cross_mob/{directory,wire,
unwire,send}` and `mobkit/peer_pubkey`, core/meerkat-desktop-gateway/src/lib.rs:311-318),
and remote mobs are a first-class product concept (`mobs.v1.registerRemoteTcp`,
`MobPlacement::RemoteTcp` in mobkit_registry.rs:74/319/752, plus thread/domain/sub-mob
creation and topology pairing).

Disposition under the directive's OTHER arm ("in some cases it might just be a simple
meta level solution that ENABLES the X capability"): cross_mob remote graduates from
cut-candidate to COMPLETE-THE-CAPABILITY. The audit's underlying defects remain real and
now read as blockers on a live consumer instead of theatre: the control-listener binding
path must be exercised end-to-end in a composition remotabot's shape can use, the
promised remote round-trip test must exist, and the "Phase 1" docs must be finished.
Cross-mob comms and remote mobs must work.

Method lesson recorded: a "no consumer" verdict is only as good as the consumer census.
The downstream questions went to the two fleets on the bus; consumers not on the bus
(desktop products) were invisible. Future capability-cut proposals must enumerate
consumers from the OWNER, not only from the operators the auditor already knows.
