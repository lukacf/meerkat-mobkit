# Manual Drill

1. Ask `incident-commander` for a `parallel status sweep`.
2. Open inspect for `payments-sre`.
3. Open routing, gating, topology, and health in separate tabs.
4. Escalate the seeded approval once, then approve the successor.
5. Split `incident-commander` into two panels and send:
   - `panel alpha follow-up`
   - `panel bravo follow-up`
