# Coverage and boundaries

[Audit index](README.md)

Baseline: `af82b6b3ab34faed9bf3e962d148d55f10dcd1dc`. Every one of the **86 tracked Markdown, MDX, documentation-text and versioned contract files** is assigned below. Every scope has an independent adjudication; final fix review is tracked in its finding ledger.

| Baseline path | Owner | Read/verification coverage |
|---|---|---|
| `.claude/commands/ship.md` | [A](A.md) | Read its entire one-line command. It is a workflow instruction, not a claim that publication has happened. No source-backed documentation defect found. No commit, push or PR command executed. |
| `.claude/skills/mobkit-platform/SKILL.md` | [A](A.md) | Read all 403 lines and loaded the skill. Checked repository map and referenced files, manifests, exports, HTTP/SSE routes, console-spawn sink and dedupe, ABAC model/engine/controller and enforcement entry points, console configuration schema and realm loader, grouping selectors and parent precedence, signed cross-mob requirements, identity-first console routing, incident example manifest/port/environment override, console build outputs, SDK commands, version checks and release automation. Compared prior audit commit 01435e7ccda5e925fc2cf9f462327bdae9584d07: it changed no scope-A file, so these are not re-reports of its repaired text. |
| `CHANGELOG.md` | [A](A.md) | Read all 5,147 lines in successive ranges, including Unreleased and every historical release section. Checked local Markdown links (none broken), current release-protocol claims against workflow/helpers, and current top-level live/voice, OIDC, dependency-pin, CMake and SDK claims against implementation. Historical API removals, superseded defaults, prior failed-release notes, missing-version explanations and incident observations were treated as dated history, not current API promises; no historical entry was reported merely because current code differs. Field measurements, old consumer acceptance and original release-time runtime behavior cannot be independently re-established from this checkout and were not represented as experimentally verified. |
| `CLAUDE.md` | [A](A.md) | Read all 59 lines. Checked every listed repository path, Python export inventory, open member-state constants, helper-module location, package/version metadata, repo-cargo cache and override logic, Makefile targets, and the distinction between local checks and hosted CI. Public exported names and state values agree with source. The shared local/full-CI overclaim is recorded once as A-012. |
| `CONTRIBUTING.md` | [A](A.md) | Read all 288 lines. Checked toolchain and SDK prerequisites, every Make target, repo-cargo behavior, and the entire release protocol against release.yml and release-candidate.py: exact-main CI, tag-only validation, five targets/three binaries, archive member shape, strict selection schemas, attestation checks, candidate/promotion/registry/assets mode matrix, publication order, original-byte recovery, add-only uploads, 30-read draft visibility loop, and local token-environment removal. Read the release hook, SDK bump script and parity verifier. Did not dispatch workflows, create tags, run publishers or reconstruct external consumer-acceptance evidence. |
| `README.md` | [A](A.md) | Read all 206 lines at baseline af82b6b3ab34faed9bf3e962d148d55f10dcd1dc. Checked identity/worker ownership against lib.rs, identity-first types and bridge surfaces; Python example constructors, builder validation, init serialization and send/wait signatures; persistent blob implementation; HTTP route declarations; manifests, release archive naming, repository paths, local links, build scripts and CI coverage. Reproduced the missing config/mob.toml failure by calling only the SDK's init-payload builder, without starting a gateway. No repository changes. |
| `docs/api/rest.mdx` | [C](C.md) | Read all 236 lines. Checked frontend and JSON route registration, aggregator prerequisites, timeline query/replay, multipart/blob handlers, auth and read-only gates against http_console.rs, runtime/console_ingress.rs, gateway composition and console_route_auth.rs. Regular file, not a symlink. Shared timeline-replay correction is recorded in C-013. |
| `docs/api/rpc.mdx` | [C](C.md) | Read all 1233 lines, in four ranges. Traced SDK initialization and role migration handling, stdio versus HTTP dispatch/capabilities, roster/lifecycle, read deadlines, topology, structural subscriptions, console send/timeline, gating, routing/delivery, memory, WorkGraph, operator verbs, storage and notifications. Read relevant parameter parsers, result types and test assertions. The earlier 01435e7 documentation changes were treated as claims, not authority. Regular file, not a symlink. |
| `docs/api/sse.mdx` | [C](C.md) | Read all 281 lines. Checked agent and mob event encodings, WorkGraph bare module wakes, structural per-client subscribe_after and stale-cursor behavior, timeline snapshot/live encoding and cursor precedence, keep-alives and shared auth. Confirmed bounded three-event inclusive RPC replay is distinct from live HTTP transports. Regular file, not a symlink. |
| `docs/archive/README.md` | [J](J.md) | Read all 131 lines at baseline af82b6b3ab34faed9bf3e962d148d55f10dcd1dc. Audited the archival disclaimer, naming/reference convention, publication boundary, every disposition entry, and the deliberately-live list. All 12 assigned archive documents are regular files, not symlinks. docs/.mintignore excludes archive/design/ and archive/proposals/; docs/docs.json redirects /plans/storage-unification-plan to the existing archived MDX page. Checked identity-context-gated mobkit/interact advertisement at meerkat-mobkit/src/rpc.rs:2037-2056, its implementation at :3608-3735, and the identity SSE route at meerkat-mobkit/src/http_console.rs:472-475. Both console package manifests still declare private=true; all three named fixture directories exist. packages/console-core/src/contract.ts:73 and console_aggregator/mod.rs:1203-1215 implement since/recent modes. Independently counted exactly 31 VALIDATED trace rows. All named MDM pack artifacts exist. CHANGELOG.md:2450-2555 records the 26-item program, including partials/refusals, and :2567-2569 confirms the two 0.8.15 stopgaps. Verified live code references to upstream-asks.md and memory-hub-roadmap.md and the memory-console proposal status. Numeric source-line pointers have drifted, but the named source paths/symbols and the substantive disposition claims remain resolvable; did not misclassify snapshot citation drift as an implementation defect. Prior audit commit 01435e7ccda5e925fc2cf9f462327bdae9584d07 has no changes under docs/archive. |
| `docs/archive/design/console-platform-direction-2026-04.md` | [J](J.md) | Read all 452 lines, including every tier, proposed RPC/stream/event shape, gating alternative, migration rule, and host extension. The opening status explicitly labels this a historical direction memo, identifies the replacement contract, and distinguishes obsolete endpoint alternatives. Verified the live routes in meerkat-mobkit/src/http_console.rs:462-483 and the shipped mobkit/interact implementation in src/rpc.rs:3608-3735. The historical claims that interact/identity lifecycle surfaces are new or absent are not current API promises and are explicitly explained by the archive index. No actionable links or misleading live instructions found; no symlink. |
| `docs/archive/design/console-progressive-customization-proposal-2026-06.md` | [J](J.md) | Read all 986 lines: motivation and boundaries, all customization levels and type sketches, phases 0-6 with checked gates/evidence, acceptance gates, and amendments. Retained the draft header, planned interfaces, historical gate outcomes, and release-workflow references as the execution record, not promises that every recorded file/line/workflow still has the same shape. Independently verified the index's current disposition: packages/console-core/package.json and packages/console-components/package.json both remain private, and reference-wrapper/configured-host-shell/custom-host-shell fixture directories all exist. No Markdown navigation links to repair. The plan is excluded from publication by docs/.mintignore; no symlink. |
| `docs/archive/design/console-timeline-scalability-plan-2026-05.md` | [J](J.md) | Read all 268 lines, including the dated execution plan, proposed wire shapes, backend/frontend/SDK requirements, test plan, rollout, and release gates. The date is 2026-05-26 and the directory index records completion and replacement, so references to the then-new v0.4 contract, cargo invocations, and proposed response semantics are historical rather than current setup guidance. Verified the surviving explicit since/recent modes at packages/console-core/src/contract.ts:73 and meerkat-mobkit/src/console_aggregator/mod.rs:1203-1215. No actionable broken links; no symlink. |
| `docs/archive/design/console-timeline-scalability-trace-2026-05.md` | [J](J.md) | Read all 74 lines, every requirement/evidence/status cell, the Typed But Unwired section, and the complete dated verification chronology. Reproducible text count found exactly 31 '\| VALIDATED \|' cells, matching the archive index; the unwired section says None. Historical test counts, branch/base SHA, contract versions, and command outcomes were preserved, not represented as tests rerun on this checkout. No actionable navigation links; no symlink. |
| `docs/archive/design/evidence/memory-survey-2026-07/README.md` | [J](J.md) | Read all 22 lines. Verified the sibling reports.md and followups.md exist and contain seven SYSTEM reports and six GAP investigations respectively. The README states the survey date (2026-07-01), repositories, MobKit snapshot, and explicit file:line staleness caveat. The live architecture document's evidence link at docs/design/agent-memory-architecture.md:17-20 resolves to this archived directory. External source checkouts named in the survey were not accessed or re-surveyed: those are historical provenance, not local runnable/current documentation dependencies. No symlink. |
| `docs/archive/design/evidence/memory-survey-2026-07/followups.md` | [J](J.md) | Read all 638 lines across all six investigations, including additional reads of lines 200-225, 420-435, and 619-638 to recover tool-truncated range tails. Covered injection echo/indexing, semantic-memory lifecycle, repeated injection budgets, Elephant truth-maintenance mechanics, Codex failed-consolidation/sentinel behavior, and the former Markdown store's lack of supersede. Preserved dated source paths, corrected preliminary hypotheses, negative findings, and proposed design takeaways as survey evidence. The parent README pins their meaning to the surveyed 2026-07-01 checkouts; current MobKit memory differences do not make these historical findings documentation defects. No actionable Markdown links or symlinks. Original external repositories and historical test outcomes were not independently reproduced. |
| `docs/archive/design/evidence/memory-survey-2026-07/reports.md` | [J](J.md) | Read all 915 lines, covering both Claude Code reports, Codex, Meerkat semantic memory, MobKit's then-current memory layers, and both Elephant reports, including all key-file inventories. Seven SYSTEM sections match the archive README. Original absolute source paths, implementation judgments, and differences between initial reports and adversarial followups are retained as historical evidence, not treated as live contract assertions. The sole Markdown-link-looking '&#91;Title&#93;(file.md)' occurrence at line 25 is inside an inline-code example of an index format, not a broken navigation link. No actual actionable links or symlinks. Historical external source claims were not re-surveyed outside the checkout. |
| `docs/archive/design/mdm-console-implementation-plan-2026-06.md` | [J](J.md) | Read all 106 lines, including architecture, completed work, generated supervisor-bridge config, version-pinning commands, smoke commands, cross-host provisioning, and published-crate status. Checked the archive index's implemented-artifact claim against examples/004-mdm-console-pack: target.rs, run.ts, scripts/local-target.sh, scripts/gcp-target.sh, browser_smoke.cjs, and ts_smoke.ts all exist. The recorded Meerkat 0.6.30 commands/results are historical June implementation evidence, not instructions to downgrade the current dependency line; the archive index points to the separate live deployment model. No actual Markdown navigation links or symlink. Did not execute provisioning, downgrades, cloud commands, or historical smoke commands. |
| `docs/archive/plans/storage-unification-plan-2026-07.mdx` | [J](J.md) | Read all 740 lines, including the warning, revisions 1-4, survey problems, principles, H1-H3, M0-M6, sequencing, and disposition ledger. Audited the maintained warning separately from the frozen plan: meerkat-mobkit/src/storage_migrate.rs:50-52 confirms the 0.8.11 retirement of checkpoint adoption and the distinct explicit 0.8.10 importer; src/identity_first/local_store.rs:1431-1450 documents/implements the remaining one-time released-envelope import; src/identity_first/adapters.rs:1142-1145 preserves first-delta conversion. src/storage_doctor.rs:376-427 shows StorageMigrator is diagnosis-only and migrate/prune are inherent concrete MobKitStorageMigrator methods; src/bin/mobkit_gateway.rs:1323-1327 dispatches storage-migrate/storage-prune. Resolved all three current-guide links and their exact headings: docs/reference/configuration.mdx:506, docs/concepts/sessions.mdx:87, docs/api/rpc.mdx:1142. The legacy URL redirect targets this existing page. Historical mutation-half/H3 proposals below the warning are expressly disclaimed, so they are not flagged as present-day API defects. No symlink; no maintenance commands executed. |
| `docs/archive/proposals/seam-assessment-mobkit-2026-08.md` | [J](J.md) | Read all 158 lines: headline invariant, eight authority rows, five compressed answers, and complete commitment queue. Kept its then-current incidents, provisional ownership/authority proposals, future 0.8.15 queue, and external consumer references intact. Verified the archive index's narrow shipped-stopgap statement against CHANGELOG.md:2567-2569, rather than assuming every proposed commitment shipped unchanged. CHANGELOG.md:2450-2555 is the later program disposition, as the index says. No actual Markdown navigation links or symlink. |
| `docs/archive/proposals/simplification-audit-mobkit-2026-08.md` | [J](J.md) | Read all 127 lines, including all 25 ranked survivors, 10 rejected candidates, keep list, deletion triggers, downstream questions, and the complete owner-evidence correction withdrawing the remote cross_mob cut. Preserved the top table and counts as the original synthesis together with the explicit later correction, not a current recommendation to remove a live capability. The follow-on 26-item program's delivery/refusals are separately recorded in CHANGELOG.md:2450-2555, matching the archive index. References to old paths and external fleets are evidence from the pinned wave-0.8.15 survey, not broken current setup instructions. No actual Markdown navigation links or symlink. |
| `docs/concepts/access-control.mdx` | [C](C.md) | Read all 260 lines. Checked the closed action vocabulary, validation, rule/anonymous/admin semantics, controller persistence, per-method HTTP mapping, SSE access views, memory read grants, lineage, and cross-mob grant parsing/authorization including host-plane verbs. Checked applicable access_control.rs assertions. Regular file, not a symlink. |
| `docs/concepts/delivery.mdx` | [E](E.md) | Read all 145 lines. Checked resolution/defaults/overrides and wildcard matching against runtime/routing.rs; trusted-resolution validation, bounded idempotency/history, rate-window accounting, adapter parsing, attempt construction and timestamp behavior against runtime/delivery.rs and runtime.rs; member-route creation against unified_runtime/edge_reconcile.rs; inspected routing_delivery.rs tests, including route-cache eviction and deterministic retry records. Regular file, not a symlink. Local documentation link targets exist. |
| `docs/concepts/events.mdx` | [C](C.md) | Read all 192 lines. Checked event envelope types, mapper/payload projection, normalization and sorting, three-event subscription implementation, WorkGraph wake shape, structural ledger projection/query and lifecycle types. Distinguished wire ConsoleFrame.payload from UI-adapter data. Regular file, not a symlink. |
| `docs/concepts/gating.mdx` | [E](E.md) | Read all 147 lines. Traced all risk tiers, conflict fallback, pending creation/notification failures, timeout refresh/clamps, approver checks, escalation and audit events through the complete runtime/gating.rs implementation and its runtime.rs types/constants. Checked authoritative configured risk tiers in rpc_gateway.rs and related gating_policy.rs tests. No independently proven defect retained. Regular file, not a symlink; local link targets exist. |
| `docs/concepts/memory.mdx` | [G](G.md) | Read all 261 lines. Traced assertion loading, normalization, querying, persistence, rollback, health checks and retention to runtime/memory.rs and runtime/bootstrap.rs; checked agent-memory configuration, SQLite recall/import/write behavior, recorder capabilities, injection composition, panel dispatch and visibility. Baseline HEAD is af82b6b3ab34faed9bf3e962d148d55f10dcd1dc. Prior audit commit 01435e7ccda5e925fc2cf9f462327bdae9584d07 is not an ancestor of this checkout: its String-literal and dispatch-taint documentation corrections are still absent here, but were explicitly excluded from new findings to honor the no-rediscovery instruction. No symlink. |
| `docs/concepts/modules.mdx` | [E](E.md) | Read all 130 lines. Checked ModuleConfig/DiscoverySpec/PreSpawnData/RestartPolicy in types.rs; module configuration resolution and terminal warning continuation in runtime/bootstrap.rs; MCP marker and per-call tool validation in module_boundary.rs/routing.rs; startup-only restart loops and health transitions in supervisor.rs; trusted-manifest defaults in decisions.rs. Inspected module_restart.rs and relevant target-contract test locations. Distinction between operational conflict ledger and separate identity memory surfaces is preserved. No independently proven defect retained. Regular file, not a symlink; local link targets exist. |
| `docs/concepts/roster.mdx` | [E](E.md) | Read all 469 lines, including both contradictory roster-context sections. Checked current gateway bridge and both SDK dispatchers/models; worker ensure and post-ensure reconciliation in rpc/mob_methods.rs; identity-roster boot/default/fallback and role-migration checks in the gateway binaries; stable encoded durable identity lowering in member_comms_id.rs and identity_first/bridge.rs; identity-first versus classic reconciliation in unified_runtime; autonomous memory exclusion against memory/coordinator.rs and identity_first_runtime.rs tests. Dependency authority is the local Cargo.toml/Cargo.lock pin to Meerkat 0.8.40, not the skill's older illustrative version or a moving upstream branch. Upstream-only lifecycle/type details were checked through local wrappers and tests, not by substituting upstream HEAD. Regular file, not a symlink; local link targets exist. |
| `docs/concepts/scheduling.mdx` | [E](E.md) | Read all 83 lines. Checked ScheduleService/store/tool composition and host target wiring in schedule_wiring.rs, persistent gateway host composition, host_runnables callback registration in Python builder/runtime/agent_builder, and removal/refusal tests in gateway_memory_config.rs (contract 0.5.0, -32602 for scheduling_files, -32601 for removed methods). No independently proven defect retained. Regular file, not a symlink; local link targets exist. |
| `docs/concepts/sessions.mdx` | [E](E.md) | Read all 117 lines. Checked operational adapter structs, constructors, aggregate JSON persistence, lock acquisition/recovery, BigQuery request/auth setup, row materialization and tests in runtime/session_store.rs and tests/session_store_jsonl.rs. Separately checked canonical storage-name/twin refusal constants in storage_layout.rs and rpc.rs, builder session-store composition, and accepted-send correlation in rpc/mob_methods.rs. Findings do not collapse the operational adapter into Meerkat's canonical session authority. Regular file, not a symlink; local link targets exist. |
| `docs/design/agent-memory-architecture.md` | [G](G.md) | Read all 1275 lines in consecutive ranges. Checked records/scopes/trust lattice, provider capability traits, SQLite layout and import, actual lexical recall, budget/dedup state, recorder/distiller/steward/hygienist boundaries, evidence capture, operator resolver activation, staged validation, profiles and CI harness. Preserved dated survey findings, original target architecture, rollout intentions, and unimplemented targets rather than treating every historical difference as a defect. Findings target contradictory current/as-built statements and material contract claims. No symlink. |
| `docs/design/hub-wire-contract.md` | [H](H.md) | Read all 91 lines; regular file, not a symlink. The memory-hub-roadmap.md Markdown link resolves locally. Treated the five Elephant surfaces, space mapping, cursor/cache, licensing discussion, and proposed provider as explicitly DRAFT/DECISION(proposed)/ASK, not promises of implemented Elephant integration. Checked the embedded shipped-state assertions: MemoryScope::Operator includes realm and operator (meerkat-mobkit/src/memory/records.rs:24-42); compose_identity_scope_set_with_bindings supplies the coordinator realm to every scope (coordinator.rs:104-140); ConsolePrincipalOperatorResolver uses the authenticated principal, with provisional opt-in and sticky identity association (coordinator.rs:145-201,416-436). No definite defect found. Elephant-side implementation or licensing approval was not independently established and is not asserted by this audit. |
| `docs/design/identity-first-doctrine.md` | [H](H.md) | Read all 221 lines; regular file. Followed ContinuityStore, lease/roster contracts, MutableRosterProvider, resume/repair paths, both gateway role-migration parsers and identity gates, Python builder/model/init serialization and shared fixture tests, and the pinned meerkat-mob 0.8.40 build and resume implementation. D1's durable-versus-worker allocation and D2/Phase 3 construction/binary-merger intentions are policy decisions, not evidence that all planned packaging work shipped. D3's exact identity map, predecessor verification, inert same-role declarations, malformed/conflicting payload handling, optional identity-plane activation, and lack of a TypeScript builder setter match the checked source. Retained the dated deployment census and historical version assertions rather than treating them as present deployment inventory. Findings distinguish policy from substrate mechanics and narrow one overbroad no-retry claim. |
| `docs/design/live-sessions.md` | [H](H.md) | Read all 789 lines; regular file. Followed live_contracts.rs, ordinary and openai-live branches of live_wiring.rs, stdio dispatch and capability catalogs, HTTP voice interception, console_voice request registry/teardown/readiness/context-status/summary composition, both gateway registrations, Python and TypeScript live parsers/connect/replacement paths, browser voice-session/context handling, and the exact registry meerkat 0.8.40 live-summary/projection/model contracts. Verified current concurrent summary policy, ExistingMember console execution versus default executor-fork policy, 15-minute audio-only silence, two-minute pending setup, 10-second backend close observation, five-second browser teardown, same-principal request fencing, strict receipts, and the documented old SDK replacement-parser limitation. Release workflow explicitly builds openai-live (.github/workflows/release.yml:392). All explicit GitHub PR/commit links returned success through gh api. Historical 0.8.38 gap observations and development lineage should be retained as dated history; current-status assertions and remaining obsolete operational guidance are separated into findings. No provider calls, audio acceptance runs, builds, or transport tests were run; reviewed tests are source evidence, not newly passing tests. |
| `docs/design/mdm-mob-target-deployment.md` | [H](H.md) | Read all 108 lines; regular file. Checked examples/package.json, local-target.sh, start-console.sh, gcp-target.sh, use-meerkat-version.sh, real-target-smoke.sh, target.rs, and run.ts. Verified default loopback 5791 target and 5790 supervisor addresses, explicit routable advertised-address requirement, binding aggregation under the pack's .state directory, local target execution, GCP sync/selected credential forwarding/build/fetch sequence, and smoke script's required peer-turn log observation. Did not start local targets, provision GCP, forward credentials, or execute the version-changing helper. The current-gate dependency/version instruction is contradicted by the actual manifest. |
| `docs/design/memory-console-ui-proposal.md` | [G](G.md) | Read all 325 lines, including the long feasibility/data-source paragraphs, all proposed widgets, phases and appendix. Preserved proposal status, the expressly named post-caf18995 snapshot, historical method/event counts and proposed surfaces; did not demand that plans match current UI implementation. Checked factual feasibility premises against panel RPC schemas, injection counters/ledger, distiller writes, transcript query shape and trust validator. The reported predicate defects contradict the stated underlying memory invariants, not merely newer UI implementation. G-015 also has an existing UI implementation consequence; runtime/UI changes are explicitly outside this audit. No symlink. |
| `docs/design/memory-hub-roadmap.md` | [G](G.md) | Read all 152 lines. Confirmed future-work framing, no active Elephant provider in the memory implementation, local-JSON legacy backend semantics, distinct bundled/session memory stores, and the bright-line boundary. Preserved hypothetical F1-F3 integration, rejected embedding direction and prospective distribution decisions. The dated Elephant PR #25 and survey-era third-party implementation claims could not be independently established from this checkout and are not reported as factual defects; no external data was sent. No symlink. |
| `docs/design/schedule-domain-adoption.md` | [H](H.md) | Read all 110 lines; regular file. Treated the RESOLVED 2026-07-09 header, explicitly dated 0.7.29 audit, and retained work items as a historical resolution/design record, not an instruction to invent or adopt a nonexistent schedule-domain API now. Verified current local attach/host/watchdog composition: schedule_wiring.rs:56,763-828; mobkit_gateway.rs:2078-2119; rpc_gateway.rs:13098-13139; gateway_composition.rs:1338-1359 uses one ScheduleService for watchdog and driver and binds the firing gate only after host availability. Cargo.lock has no meerkat-rpc package. The current code additionally has a firing-intent gate, so the old 'incidentally' assessment is not treated as a current invariant. Historical HomeCore stranded-row counts and the behavior of released 0.7.25 were not rerun; no defect is inferred merely from historical design text differing from today's code. |
| `docs/design/upstream-asks.md` | [I](I.md) | Read the entire 1,869-line file, including the introductory status/adoption summary, asks 1–35, Studio M1–M4, and the separately numbered 21c and 21b/21d addenda. Counted 40 top-level ask headings; the stated count is correct. The file is not a symlink. Both Markdown relative-link targets exist and are not symlinks: agent-memory-architecture.md and ../archive/design/evidence/memory-survey-2026-07/followups.md. Link targets were existence-checked, not audited. Applied the file's historical/current boundary: old incident narratives, proposed shapes, version-qualified interim behaviors, old line-number citations, the 0.7.30 three-retained-bodies example, and historical CLI/tool names are not defects merely because current code differs. In particular, no evidence establishes that an upstream runtime ask must be reopened. The two findings concern an explicitly outstanding downstream follow-up that is implemented and a supposedly source-verified historical fact that the exact historical source disproves. The latter is not a request to rewrite an accurate historical account to today's API. |
| `docs/design/voice-handover.md` | [H](H.md) | Read all 200 lines; regular file. This explicitly dated emergency WIP checkpoint is historical, so its old Git pins, failed S99 acceptance, unpublished local test totals, draft/release gates, pending ordinal repair, local demo hash/state, and resume instructions are not rewritten as current release assertions. Compared durable context-status protocol and its 13 reasons with console_voice/context_status.rs and console_voice_v1.json; current implementation retains that carrier. Checked current summary/ExistingMember composition, SSE identity work references, shared-owner composition, and browser module paths. PR #408 and the immutable ASYNC_VOICE_CONTEXT_HANDOFF.md link resolve; the additionally named VOICE_LEGACY_WORKSPACE_HANDOFF.md exists at its stated immutable commit. No definite defect found within the checkpoint's explicit historical boundary. Historical paid-test outcomes and remote credentials were not accessed or revalidated. |
| `docs/design/workgraph-integration.md` | [H](H.md) | Read all 207 lines; regular file. Treated the 0.7.23 survey and Stages A-E as the historical 0.7.30 implementation plan, not a present declaration that every listed source line, API signature, verification run, or release checklist remains current. Traced implemented service/tool/grant composition, bootstrap and nested Mob MCP propagation, schedule-host overlay forwarding, RPC read/mutation maps, authenticated confirmation and break-glass ownership, SDK methods/builders/results, inline card folding, headless action/revision helpers, and workgraph RPC/e2e test source. WorkGraph due_at remains eligibility rather than a deadline, confirmed in the exact pinned generated workgraph lifecycle guards. The universal CAS claim is false independently of version framing and is recorded; changed realm/grant behavior is addressed as a current contract qualification in the wire-contract finding rather than rewriting the historical plan. |
| `docs/design/workgraph-wire-contract.md` | [H](H.md) | Read all 182 lines; regular file. Checked every listed method against rpc/workgraph_methods.rs and the pinned meerkat-workgraph 0.8.40 request/service types, result wrappers, codes, ABAC/read-only gates, principal promotion, owner/status/target normalization, alias occupancy admission and sidecar, SDK names/results/options, capabilities, experience projection, and console action helpers. This is version-titled history but also calls itself the binding contract and is still referenced by current handler and SDK source; finding H-010 therefore asks for an explicit current-scope qualification while preserving 0.7.30 as-built history. Current WorkGraph additionally has the facts RPC and facts SSE (workgraph_methods.rs:66-75,618-631; http_sse.rs:52,834-855); the explicitly version-qualified 'no push events at meerkat 0.7.23' sentence is not falsely charged as a historical error. It is useful only as historical upstream context, not proof of current absence. Confirmed the literal-target/alias occupancy split, coordinate-only reassign, console-only authenticated break-glass, principal evidence restrictions, and default close status. Findings cover CAS breadth, current scope restrictions, and actual conflict retry responsibility. |
| `docs/guides/authentication.mdx` | [C](C.md) | Read all 285 lines. Checked console_auth_config.rs, auth/mod.rs, http_auth.rs, runtime/console_ingress.rs and HTTP/SSE token extraction; inspected jwt_oidc_auth.rs, jwks_cache_and_auth.rs and sse_auth_gate.rs coverage. Fixed trusted snapshots, OIDC verified-email behavior, HS256 development restriction, independent JwksCache issuer/audience handling and provider classification agree with implementation. No proved document defect. Tool output masks Authorization examples, but a read-only literal check confirmed that the files contain Bearer syntax, not the displayed asterisk masks; no false finding was raised. Regular file, not a symlink. |
| `docs/guides/console.mdx` | [F](F.md) | Read all 719 lines. Checked routes and dispatch against http_console.rs, http_sse.rs and unified_runtime/http.rs; timeline envelopes and idempotency against console_aggregator; gateway init, persistence and voice registration against both binaries; console config against console_config.rs and its consumers; voice readiness, context/status, summary, silence/teardown constants and paid/offline runner commands; WorkGraph graph/tree and topology CAS/bulk contracts; CSS, themes, resizers, package boundaries, exports, production build and Vite proxy. Internal documentation links resolve. Not a symlink. Baseline is af82b6b3ab34faed9bf3e962d148d55f10dcd1dc. Per the brief, did not re-report the two /console/modules descriptions or incomplete ConsolePolicy literal already corrected by 01435e7ccda5e925fc2cf9f462327bdae9584d07. That prior commit is NOT an ancestor of this baseline (git merge-base --is-ancestor returned 1), so its corrections are still absent in the audited file and must be retained/integrated separately. Validation was read-only source/manifest/static-contract inspection; no build, dependency install, browser/provider call, or source modification. |
| `docs/guides/deployment.mdx` | [B](B.md) | Read all 292 lines. Regular file, not a symlink. Followed both gateway init/bind/registry/resume/shutdown paths, shared GatewayHttpBinding and exposure policy, Python/TypeScript URL getters and setters, library run/serve signatures, schedule watchdog classification/cadence, tracing and panic hooks. Verified the system Opus/pkg-config/OPUS_STATIC advice against audiopus_sys 0.2.2 build.rs fetched from the exact locked crates.io archive in memory, not against current upstream main. Docker snippets are deployment sketches, not complete configured nginx deployments; no Docker images were built. Python syntax and all local links pass. Important implementation caveat: mobkit_gateway now applies auth_config to its served decision state, but its earlier exposure gate still evaluates the open default state, so auth_config alone does not replace allow_remote on that binary. |
| `docs/guides/flow-editor.mdx` | [F](F.md) | Read all 213 lines. Traced all documented hosting modes, routes, binary flags/environment defaults, authoring method names, runtime catalog composition, SDK wrappers/transports, ABAC/deploy gates, draft path/guards/autosave recovery, rendered source paths, import/export, deploy argv and output parser, navigation, deep links and host events. Verified esbuild/react-global composition, static dev server RPC proxy, Make targets, typecheck/boundary/export-manifest tests. Internal links resolve. Not a symlink. No rkat execution or draft-store mutation was performed; current upstream behavior was inferred only where locally implemented or asserted by local tests. |
| `docs/guides/module-system.mdx` | [E](E.md) | Read all 189 lines. Followed UnifiedRuntime::route_module_call through module_ops, runtime/routing.rs, module_boundary.rs, supervisor.rs, process.rs and normalization/protocol parsing. Checked MCP close/timeout handling, generic payload projection, no stdin request forwarding, startup health/restart policies and public error names. Regular file, not a symlink; local link targets exist. |
| `docs/guides/unified-runtime.mdx` | [E](E.md) | Read all 366 lines. Checked builder defaults and legacy required fields, staged bootstrap/rollback, classic versus identity reconciliation, route wiring, ordered teardown and event draining, reference HTTP composition, live composition validation/caching/auth, and ErrorEvent definitions/emission/probe tests. Read-only audit at HEAD af82b6b3ab34faed9bf3e962d148d55f10dcd1dc; inspected relevant prior-change scope for 01435e7ccda5e925fc2cf9f462327bdae9584d07. No repository edits or git mutations. Tests were inspected, not executed: the requested wave permits repository reads only, and builds/tests would create files or subprocess state. Some historical integration tests cited below are #&#91;ignore&#93;, explicitly identified where used; source is the primary proof. Regular file, not a symlink; local link targets exist. |
| `docs/introduction.mdx` | [B](B.md) | Read all 116 lines. Regular file, not a symlink. Checked the ownership/composition description against src/lib.rs, mob_handle_runtime.rs, gateway_composition.rs, storage_provider.rs and unified_runtime; checked the Python identity-handle calls against sdk/python/meerkat_mobkit/runtime.py. Python snippet passes AST parsing; all seven linked local/external destinations were inventoried and all local destinations exist. No independent defect found in this introductory-level description. Audit baseline is af82b6b3ab34faed9bf3e962d148d55f10dcd1dc. Validation throughout this report was read-only source inspection plus Python/TOML syntax and local-link checks, not a live LLM, HTTP deployment, or compile/run of illustrative snippets with unspecified host variables. |
| `docs/quickstart.mdx` | [B](B.md) | Read all 216 lines. Regular file, not a symlink. Inspected relevant changes from 01435e7ccda5e925fc2cf9f462327bdae9584d07; the current roster-context implementation has outgrown that documentation correction. Verified version 0.8.39 against the workspace, release archive names and Linux glibc floor against release.yml and the portability gate, repo-cargo output-path behavior, module-only Rust bootstrap signature, SDK build/status/shutdown signatures, identity spec/topology providers, completion-cursor wait methods, respawn/reset and role-migration entrypoints. All three Python/TOML blocks pass syntax parsing after removing Markdown-container indentation. Local page and anchor targets were checked. Findings B-001/B-002 and the repeated mode claim in B-017 require corrections. |
| `docs/rct/console-rest-sse-contract-v0.1.0.json` | [C](C.md) | Read all 44 lines and parsed JSON. Historical 0.1.0 snapshot, not the current release contract; the old POST /interactions/stream entry was not reported merely because today's routes differ. Regular file, not a symlink. |
| `docs/rct/console-rest-sse-contract-v0.2.0.json` | [C](C.md) | Read all 47 lines and parsed JSON. Preserved historical 0.2.0 route/schema facts rather than treating them as current promises. Regular file, not a symlink. |
| `docs/rct/console-rest-sse-contract-v0.3.0.json` | [C](C.md) | Read all 143 lines and parsed JSON. Historical version, including breaking-change and old error-code history. Checked spec_source references; absent historical source targets are covered by C-028. Regular file, not a symlink. |
| `docs/rct/console-rest-sse-contract-v0.4.0.json` | [C](C.md) | Read all 207 lines and parsed JSON. Historical timeline-window contract, not rewritten to today's implementation. Checked spec_source references; shares C-028. Regular file, not a symlink. |
| `docs/rct/console-rest-sse-contract-v0.5.0.json` | [C](C.md) | Read all 1636 lines in four ranges and parsed JSON. This file explicitly identifies itself as the current canonical contract and is included by console_route_auth.rs. Compared its REST/RPC/SSE methods, required/optional fields and errors with current handlers/types. checked_contract and previous_contract targets exist. All assigned MDX local page links resolved. Caveats for the whole audit: static source/test review, no live gateway requests or compiled tests run; test quotations are existing assertions, not claims of test execution. No repository files or git state were modified. All findings are documentation corrections, not proposed runtime changes. |
| `docs/reference/architecture.mdx` | [B](B.md) | Read all 306 lines. Regular file, not a symlink. Checked crate/module paths, dependency ownership, gateway compatibility profiles, console persistence difference, MCP tool names and connect/list/call/close mechanics, first-event subprocess supervision and restart budgets, timestamp/event_id/source sorting, merged HTTP route families and protected Flow Editor, storage provider durability checks, signed-contact seams, routing-resolution validation and fixed-window delivery limits. No independent material defect identified at the architecture page's abstraction level. All local links exist. Diagram and prose are architectural summaries, not exhaustive route or crate inventories. |
| `docs/reference/configuration.mdx` | [B](B.md) | Read all 1,032 lines in contiguous ranges. Regular file, not a symlink. Compared module types/defaults, the full rpc_gateway runtime_options allowlist and relevant nested parsers, both binaries' typed/untyped init paths, model validation, host-config loader, one-shot spec update and role migrations, identity bootstrap/roster, WorkGraph placement/scope/namespace grants, storage layout/census/provider declarations, console schema/realm overlay fields, policies, local JSON ledger, agent-memory configuration/coordinator/importer, constants and SDK emitters. Reviewed exact pinned meerkat-mob/meerkat-core 0.8.40 archive source for profile tools, TOML diagnostics, self-hosted config and model fallback (not the unrelated upstream audit branch as authority). Nine Python/TOML blocks pass syntax checks after Markdown dedenting; all local links exist. References to public APIs were checked statically; no gateway subprocess was started because the task requires a read-only audit. Findings below distinguish real current behavior from outdated claims, including accepted compatibility aliases. |
| `docs/reference/decisions.mdx` | [B](B.md) | Read all 241 lines. Regular file, not a symlink. Checked every policy table, struct field, rule ordering, error spelling, empty trust manifest behavior, BigQuery character validation, four release targets/support matrix, build_runtime_decision_state validation order, conditional OIDC validation, local_console construction and with_modules against decisions.rs, runtime.rs and auth/mod.rs. Checked console ingress's optional-principal path for the open-console/ABAC caveat. No independent defect found. Rust blocks using already-defined policy/JSON variables are explanatory snippets rather than complete programs. All local links exist. |
| `docs/reference/governance.mdx` | [E](E.md) | Read all 103 lines. Read complete governance.rs and baseline.rs, governance_check binary and governance_contracts.rs tests. Checked API signatures, supported status values and evidence rules, exact error enum, CLI input selection, baseline path resolution and the text-scanning verifier's actual strength. The previously corrected explicit-path/MEERKAT_REPO behavior is not reported again. Regular file, not a symlink; local link targets exist. |
| `docs/sdks/python.mdx` | [D](D.md) | Read all 810 lines at baseline af82b6b3ab34faed9bf3e962d148d55f10dcd1dc. Regular file, not a symlink. Checked all 23 Python fenced blocks for syntax and every meerkat_mobkit import against the installed-from-source export surface using PYTHONDONTWRITEBYTECODE=1 and PYTHONPATH=sdk/python. Checked 32 documented builder names and 36 advanced/mobpack method names against AST inventories, plus delivery, observation, lifecycle, identity handles, structural event/run models, provider protocols, session-builder/customizer callbacks, detached jobs, storage/error models, logging, config serializers, and gateway handlers. All documented imports resolved. Reproduced the event attribute failures, default DurableAgentSpec payload, disconnected storage example, and cursor-only completion behavior without starting a gateway or writing repository files. Verified local link targets and referenced heading anchors. IMPORTANT BASELINE CAVEAT: 01435e7ccda5e925fc2cf9f462327bdae9584d07 is NOT an ancestor of HEAD (git merge-base --is-ancestor returned 1), and its SDK corrections are absent from this checkout. Per the instruction not to rediscover its fixed items, this report excludes its already-known missing quickstart member creation/configuration, stale 25-variant count, and stale pinned-0.8.32 wording; those prior corrections still need integration separately. Rejected an overbroad OIDC-unavailable candidate: auth.google emits the still-unsupported provider=google, while raw provider=oidc is now accepted; the document's negative wording is specifically about helpers, so it does not prove a denial of raw OIDC support. |
| `docs/sdks/rust.mdx` | [D](D.md) | Read all 324 lines. Regular file, not a symlink. Checked the 0.8.39 installation version against workspace.package.version, public exports in lib.rs, all displayed config/event type shapes, module startup/routing signatures, UnifiedRuntime builder methods, SQLite memory opening/import behavior and remember/recall/forget request/result signatures, decision/auth/governance functions, console routers, HTTP binding and exposure helpers, and local links/anchors. No new independently counted Rust-page defects beyond the prior fix commit. IMPORTANT BASELINE CAVEAT: the already-corrected governance signatures and string-valued validate_http_bind_policy surface argument from commit 01435e7ccda5e925fc2cf9f462327bdae9584d07 remain wrong in this baseline because that commit is not an ancestor; they are deliberately not duplicated in this wave's finding count. No Rust compilation or live gateway/model calls were performed: validation is against public definitions and existing regression-test source, with historical git objects where needed. |
| `docs/sdks/typescript.mdx` | [D](D.md) | Read all 353 lines. Regular file, not a symlink. Checked installation/client-only packaging against package.json, ESM/CJS exports, all named imports and public interfaces against index.ts/types.ts, builders and init serialization, detached job registration/results/credentials, memory and provider options, structural event/run parsing, mobpack methods/results, role-migration absence, HTTP exposure and host config forwarding, storage census/error translation, logging, error callbacks and HTTP transport timeout. Checked local links/anchors. A mechanical comparison found actor_loop_terminated missing from the document's purported ErrorCategory list. TypeScript node_modules were absent in both the SDK and repository root; no dependencies were installed, build artifacts produced, or TypeScript compilation claimed. The raw-memory camelCase issue is specifically about passing a plain object; memory.localJson({healthCheckEndpoint: ...}) is valid and correctly converts to snake_case. |
| `examples/001-incident-command-center-pack/README.md` | [F](F.md) | Read all 65 lines. Checked scenario, server, Rust runtime/tool fixture source, browser/TS/Python smoke references, topology launcher and browser phases, durable topology state and timeout/artifact environment variables. Verified Cargo example registration and provider-backed versus TestClient offline modes. Identified stale messaging surface descriptions. Not a symlink; no live model or offline stateful launcher executed. |
| `examples/001-incident-command-center-pack/prompts/manual-drill.md` | [F](F.md) | Read all 9 lines. Checked incident-commander/payments-sre scenario identities, seeded gating escalation/approval, routing/topology/health controls, details/inspect view and multi-panel follow-up flow against scenario and browser smoke. 'Inspect' is still an actual details panel (reachable via the Details affordance), not a removed feature. No independent defect found. Not a symlink. |
| `examples/002-foresight-studio-pack/README.md` | [F](F.md) | Read all 42 lines. Checked launcher smoke/browser/live branches; rosterProvider/topologyProvider/agentCustomizer/sessionService/consoleConfig builder usage, callback tool implementations, config paths and scene data, provider fallback and --real-llm/--demo-llm/--kickoff behavior. With OPENAI_API_KEY the implementation also selects the real provider without --real-llm, but the documented explicit flag is valid. Shared console-dependency prerequisite issue is recorded once under examples/README.md. Not a symlink. |
| `examples/002-foresight-studio-pack/prompts/board-readiness.md` | [F](F.md) | Read all 12 lines. Checked Borealis/Q3/lighthouse scenario, director kickoff, evidence/scorecard/red-team/board memo callbacks and peer topology. This is an operator prompt, not a guaranteed deterministic provider result. No defect found. Not a symlink. |
| `examples/003-swarm-stress-pack/README.md` | [F](F.md) | Read all 58 lines. Checked scenario arithmetic (4 x 75 baseline, 4 x 3 parents x 20 burst, 150 peers, 240 concurrency), Gemini key/model selection, explicit shape-only options, structural/browser launchers, timeline reply assertions, screenshots, live choreography, persistence path and dense-edge restart conditional. Real stress execution was intentionally not run. Not a symlink. |
| `examples/004-mdm-console-pack/README.md` | [F](F.md) | Read all 163 lines. Checked external binding construction, target/hive roster/topology, target.rs and Cargo registration, local/GCP helper flags and defaults, env forwarding, supervisor/member address controls, start-console and --run modes, real-target smoke/e2e/multi commands, dependency update script and current exact Meerkat manifest pins. Preserved historical 0.8.2 acceptance as historical rather than claiming a new paid run. No provisioning, persistent target process, or version update was executed. Not a symlink. |
| `examples/005-access-control-pack/README.md` | [F](F.md) | Read all 77 lines. Checked server TestClient construction, all seeded identities/labels/rules and persona visibility/send/admin table, require_app_auth=false and dev issuer configuration, launcher serve/smoke modes, four proxy ports/token behavior, smoke request/status assertions and runtime ABAC live persistence/refresh path. Shared missing console build prerequisite reported once under examples/README.md. Not a symlink. |
| `examples/README.md` | [F](F.md) | Read all 62 lines. Enumerated all five packs; checked referenced launcher paths and package.json scripts, cwd assumptions, console build prerequisites, and 300+240 stress constants. Read-only createRequire(console/build.cjs).resolve('esbuild') returned MODULE_NOT_FOUND in this checkout. Not a symlink. Launchers were not executed: they install/build/write state, some write platform temporary directories, live packs incur provider costs, and GCP provisioning is out of scope. |
| `meerkat-mobkit/src/memory/distiller_prompt_v0.md` | [G](G.md) | Read all 87 lines. Checked all placeholders against render_prompt/render_transcript, action and target_id parsing, all seven kinds, epistemic variants, evidence ranges, manifest/tombstone rendering, and remember/supersede execution. No demonstrable prompt/schema mismatch found; no prompt redesign proposed. No symlink. |
| `meerkat-mobkit/src/memory/hygienist_prompt_v0.md` | [G](G.md) | Read all 73 lines. Checked rendered role labels and protected-range placeholders, JSON object/ops schema, prune_tool_results and collapse names, exclusive end indices, replacement requirement, role constraints and quarantined-span validation. Prompt matches the parked internal engine; the runnable calibration lane is not evidence that the public gateway enables the engine. No demonstrable prompt mismatch. No symlink. |
| `meerkat-mobkit/src/memory/steward_prompt_v0.md` | [G](G.md) | Read all 216 lines. Checked four phase markers/placeholders, GatherRequest, UsageVerdict, RawStewardOp/RawScope, proposal/quarantine/open-loop/contradiction/working-set outputs and harvest verdicts against steward.rs; checked verification retier exception, quarantine release copying, operator routing and staged ceilings. The prompt's scope kind/key shape is intentionally parsed then expanded, not the serialized MemoryScope shape; no false schema finding raised. No demonstrable prompt mismatch. No symlink. |
| `meerkat-mobkit/tests/fixtures/README.md` | [F](F.md) | Read all 96 lines. Enumerated every named fixture, checked consumers in adapters.rs, identity_first_lazy_recall_continuity.rs, released_realm_upgrade_drive.rs, bridge.rs and Python tests. Verified both forensic SHA256 manifests against unchanged bytes. Read-only stat checks matched all pinned main/WAL sizes: baseline 4096/498552, burst_drain 4096/828152, crash_sigkill 4096/828152, deploy_cycles 4096/444992. No SQLite connection was opened against corpus files, and no fixture was regenerated. Provenance of external historic captures cannot be independently recreated locally; local hashes, tests and embedded contracts were used. Not a symlink. |
| `memory-evals/README.md` | [G](G.md) | Read all 194 lines. Checked runner CLI stages, schema validation, profile/fixture formats, deterministic-vs-judgment failure policy, mock/live fallback code and integration test inventory. Historical Selector seam/run log remain historical, not supported command promises. scripts/memory-evals --check passed: 4 profiles, 10 Selector, 5 Distiller, 5 Steward, 4 Hygienist and 5 invariant fixtures. An in-memory stub of the eval subprocess independently exercised no-auth child exit 3 and verified normal wrapper exit 0 after mock fallback; no live LLM calls or Rust builds were run. No symlink. |
| `memory-evals/prompts/distiller-v0.md` | [G](G.md) | Read the identical 87-line prompt through the embedded copy; cmp verified byte-for-byte equality to meerkat-mobkit/src/memory/distiller_prompt_v0.md. Checked profile prompt_bundle resolution and runner placeholders. Same schema/parsing review as embedded copy; no finding. No symlink. |
| `memory-evals/prompts/hygienist-v0.md` | [G](G.md) | Read the identical 73-line prompt through the embedded copy; cmp verified byte-for-byte equality to meerkat-mobkit/src/memory/hygienist_prompt_v0.md. Checked profile resolution and calibration fixture role/op conventions. No finding. No symlink. |
| `memory-evals/prompts/selector-v0.md` | [G](G.md) | Read all 59 lines. Checked placeholder/output schema against preserved Selector fixture validation. This is explicitly retired calibration evidence per README, not a live runtime prompt: no request to restore or redesign the Selector. Historical selection policy retained. No symlink. |
| `memory-evals/prompts/steward-v0.md` | [G](G.md) | Read the identical 216-line prompt through the embedded copy; cmp verified byte-for-byte equality to meerkat-mobkit/src/memory/steward_prompt_v0.md. Checked profile and mock/live consolidate seam. No finding. No symlink. |
| `packages/console-components/README.md` | [F](F.md) | Read all 204 lines. Checked every named component, hook, helper and type against src/index.ts, stylesheet entry/domain files, private-package exports, theme token activation, dock controller scope, and topology editable/read-only/disabled, pair capabilities, authority-revision map and bounded bulk logic. The list is a curated subset rather than an explicit exhaustive export inventory, so additional exports were not reported as omissions. Not a symlink. |
| `packages/console-core/README.md` | [F](F.md) | Read all 59 lines. Checked curated src/index.ts exports, package.json private/root/subpath exports (including ./runtime), dock/controller/headless/network ownership, and useConsoleDockController consumer recipe. No independent defect found. Not a symlink. |
| `packages/flow-editor-components/README.md` | [F](F.md) | Read all 69 lines. Checked module/barrel exports, JSX globals and esbuild composition, facade access timing, boundary-test restrictions, no ts-nocheck in component TSX, private-package status, exported-but-unmounted floating TweaksPanel versus full-page settings. Interpreted 'no document state' as no independent authoritative document owner: useStudioState does hold client-side projection copies/history, but authoring operations remain server-owned; not raised as an inadequately distinguished defect. Not a symlink. |
| `packages/flow-editor-core/README.md` | [F](F.md) | Read all 68 lines. Checked module map, package aliases, RPC fetch ownership, facade construction and actual manifest cardinality, esbuild test bundle path, private exports, tsconfig and ts-nocheck distribution. All non-barrel/non-shared source modules still carry ts-nocheck as described. Historical extraction/migration statements were not treated as current behavioral guarantees. Not a symlink. |
| `scripts/memory-bright-line-allow.txt` | [G](G.md) | Read all 28 lines. Checked allowlist syntax, pattern names, exact per-file allowances and scanner scope against scripts/check-memory-bright-line, then chased HnswDiscardSource reads and lifecycle deletion. scripts/check-memory-bright-line passed for 18 source files. Relative Markdown link scan across all 13 assigned paths found no missing local targets. All 13 paths are regular files, not symlinks. Only the requested JSON artifact was written; repository files and git state were not changed. |
| `sdk/python/CHANGELOG.md` | [D](D.md) | Read all 332 lines. Regular file, not a symlink. Treated 0.8.19/0.8.16/0.8.9/0.8.0/0.7.17/0.6.29/0.6.28/0.6.6/0.6.0 entries as release history rather than current API promises. Cross-checked role migration, bounded helpers and scheduling removal, stderr/shutdown/detached jobs, bootstrap/lease semantics, memory, event/run results and errors, signed peers, and removed RPC names against source and relevant historical changes. Preserved historical in-memory mob-storage, title-cased state and missing helper-session-id statements despite present behavior differing. Investigated the reconcile section against its introducing commit 63f293c7b49af3cef07579e2a8ae0b53014603de and the available v0.6.6 release tag, proving the SDK attribution was already false then rather than merely becoming outdated. A v0.6.0 tag is not available locally, so no claim is made that it was checked. No changelog entries were changed or release coverage omissions invented. |
| `sdk/typescript/CHANGELOG.md` | [D](D.md) | Read all 304 lines. Regular file, not a symlink. Applied the same historical boundary as the Python changelog, including the intentional 0.8.6 TypeScript versus 0.8.9 Python detached-job attribution. Checked the cited packaging/error/transport/parser fixes and present counterparts, without treating later changes to mob persistence, state casing, shutdown protocol details, helper results or wrapper availability as historical defects. The shared reconcile misattribution is consolidated into D-008, with source from the introducing commit and v0.6.6 rather than current-source-only reasoning. Validation remained read-only: no repository changes, git-state changes, delegation, live models, package installs, or generated source/build artifacts. |

## Additional surfaces and ownership

`docs/docs.json` navigation (27 pages) and its redirect target were checked; `docs/.mintignore` preserves unpublished internal records and the published archived plan. All 28 tracked MDX documents compile, including the archived plan.

The repeated K-005 claim was also corrected in the Python `RosterContext` docstring and TypeScript API comment. Python AST comparison after removing docstrings and TypeScript comparison after removing documentation comments show no behavioral changes. The memory bright-line allowlist changes explanatory comments only, not allow entries.

No `.codex`, `.agents`, or `AGENTS.md` is present in the tracked baseline. The hidden `.claude` command and MobKit skill are covered above. Its three tracked skill aliases are external symlinks. A supplemental content audit read all three external SKILL.md files (1,261 lines) and checked twelve referenced resources for MobKit integration correctness. The upstream-owned MobKit lease finding is recorded separately as SKILL-001; no shared external target was edited through a symlink.

### `.claude/skills/banana-skill`

Tracked mode-120000 directory symlink. Exact target: /Users/luka/.codex/skills/banana-skill. Target exists on this host and contains SKILL.md. The target is outside this checkout; coverage here is the repository-owned alias, not an audit of independently installed external skill content. No external referent edited.

### `.claude/skills/meerkat-architecture`

Tracked mode-120000 directory symlink. Exact target: /Users/luka/.codex/skills/meerkat-architecture. Target exists on this host and contains SKILL.md and references. The target is outside this checkout; coverage here is the repository-owned alias, not an audit of independently installed external/upstream architecture documentation. No external referent edited.

### `.claude/skills/rct-lite`

Tracked mode-120000 directory symlink. Exact target: /Users/luka/.codex/skills/rct-lite. Target exists on this host and contains SKILL.md, agents and references. The target is outside this checkout; coverage here is the repository-owned alias, not an audit of independently installed external methodology content. No delegation performed and no external referent edited.

## External skill content supplement

### `.claude/skills/banana-skill/SKILL.md`

Read all 175 SKILL.md lines through the MobKit alias, including runtime path, model/reference-image flags, output/inspection conventions, transparency, sprites and runtime prerequisites. Read banana.py:1-110, 840-870 and 944-953 to establish the entrypoint and the side-effect-free help path; did not audit provider-generation algorithms.

No MobKit-specific API or integration assertion occurs in this general-purpose image skill. Its personal installation path is present on this host; portability to an unrelated host was not assumed. General image-generation guidance was not re-audited. No finding.

**`.claude/skills/banana-skill/SKILL.md:13-18`**

```text
BANANA_PY="/Users/luka/.codex/skills/banana-skill/scripts/banana.py"
python3 "$BANANA_PY" --help
```

The hard-coded canonical runtime path exists and is the same script reached through the MobKit alias.

**`.claude/skills/banana-skill/scripts/banana.py:68-96`**

```text
parser = argparse.ArgumentParser(prog="banana", description="Banana Python CLI")
```

The actual argparse entrypoint exposes the documented invocation and flags. main calls parse_args before reading keys or beginning generation.

**`python3 -B .claude/skills/banana-skill/scripts/banana.py --help`**

Confirms the local alias entrypoint works without generating images, consulting shell startup files, sending provider requests, or writing bytecode.

> Exit 0; printed Banana Python CLI usage including --reference-images, --model {nano,pro,2}, --output-dir, --transparent and sprite options.
### `.claude/skills/meerkat-architecture/SKILL.md`

Read all 696 SKILL.md lines through the MobKit alias. The initial read was truncated; subsequent overlapping reads of lines 300-495 and 496-697 completed the entire file. Verified all seven named reference files exist and are readable. Searched their contents for MobKit/mobkit/UnifiedRuntime, then read gotchas.md:32-55 and crate_map.md:148-253 around the downstream claims. Traced the concrete MobKit lease claim to current local implementation and tests. Upstream-only API details were scoped to the supplied upstream coverage ledger rather than re-audited.

One concrete current MobKit-specific inaccuracy exists in the upstream-owned gotchas reference, not in the MobKit alias wiring. See SKILL-001. No external file edited.

**`.claude/skills/meerkat-architecture/references/crate_map.md:168`**

```text
downstream remote/mobkit providers
```

The downstream provider assertion is supported by mobkit-store-conformance/src/reference.rs:466-472, which defines ReferenceMemoryRealmProvider and implements meerkat::storage_provider::RealmStorageProvider.

**`mobkit-store-conformance/src/reference.rs:466-472`**

```text
impl meerkat::storage_provider::RealmStorageProvider for ReferenceMemoryRealmProvider {
```

Concrete local implementation behind the external skill's downstream-provider statement.

**`meerkat-mobkit/src/identity_first/local_lease.rs:83-108`**

```text
let token = FencingToken::new(state.next_token);
            state.next_token += 1;
```

The skill's core rule about explicit local acquisition minting a fresh fence remains correct. SKILL-001 must not be used to make acquire_leases idempotent.

**`meerkat-mobkit/src/unified_runtime/builder.rs:1229-1242`**

```text
LocalContinuityStore::open_with_fencing_floor(continuity_path.clone())
```

The builder consumes the persisted high-water and passes it to LocalLeaseProvider::with_floor(high_water), supporting the separate restart-fencing caveat. No regression is alleged there.
### `.claude/skills/rct-lite/SKILL.md`

Read all 390 SKILL.md lines through the MobKit alias, including setup, requirements/statuses, real-entrypoint evidence, gate reviewer loading, handoffs and anti-patterns. Checked all four named resources for existence/readability and scanned their contents for MobKit, installation paths and project-specific command assumptions. Read rct-theory.md:55-68 and 185-202 and spec-compliance.md:32-47 around the generic Rust examples.

No MobKit API claims or broken required resource paths found. This was a content audit, not an RCT invocation: no implementation/reviewer agents were launched, no phase artifacts were created, and no generic methodology re-audit was undertaken.

**`.claude/skills/rct-lite/references/rct-theory.md:194-201`**

```text
cargo test -p my-crate
```

This is a generic placeholder comparison, not an assertion that MobKit ships a package named my-crate.

**`.claude/skills/rct-lite/agents/spec-compliance.md:41-46`**

```text
Identify the project language and test tooling:
```

Generic tooling examples need project adaptation. MobKit's explicit CLAUDE.md:38 policy requires scripts/repo-cargo; no local invocation failure or MobKit-specific false command was demonstrated.

**`CLAUDE.md:38`**

```text
All Rust commands go through `scripts/repo-cargo`, which isolates `CARGO_HOME` and `CARGO_TARGET_DIR` per repo/worktree.
```

Repository-owned build policy supplies the concrete Rust invocation. Generic upstream methodology examples were not counted as separate local defects.
### `.claude/skills/mobkit-platform/SKILL.md`

Loaded the current skill at task start. Detailed source-contract coverage is inherited from audit-A.json; not repeated in this narrow supplement. The current file has 419 lines after concurrent scope-A corrections.

The fourth discovered skill was already fully audited by scope A. Existing findings/fixes are not duplicated here.

### `.codex`

Checked the repository path directly: neither .codex nor .codex/skills exists.

There is no omitted repository-local Codex skill directory. The three tracked .claude aliases point into the separate personal /Users/luka/.codex/skills installation. Absence of an optional repository .codex directory is not a broken alias or an unreadable document.

### Upstream ownership and previous coverage

```json
[
  {
    "topic": "Upstream architecture coverage is established, but live alias bytes differ",
    "evidence": "At bdb53a24f7a1ac048fe91a328f82e161e00a2b95, audit/docs-review-2026-09-21/coverage.md:27-34 explicitly covers meerkat-architecture/SKILL.md and all seven references. git byte comparisons found that each live file differs from that pin; the live checkout's tracked architecture directory is clean.",
    "consequence": "The pin proves upstream ownership and prior audit coverage, not that the personal alias currently exposes the audit branch's corrected contents. Existing upstream-only API/build findings were not re-reported as new MobKit inaccuracies."
  },
  {
    "topic": "The new MobKit finding was excluded from the upstream source audit",
    "evidence": "Upstream coverage.md:31 states: 'External meerkat-mobkit implementation/test claims cannot be fully checked in this checkout.' A02.md:2971 states: 'The historical meerkat-mobkit source/test claims in gotchas.md concern an external repository not present in this checkout. They were not treated as missing local files or disproved without external evidence.'",
    "consequence": "SKILL-001 is a genuinely downstream-specific coverage gap, established against local MobKit source and tests. Gotchas item 37 at line 48 is byte-identical in the live alias and supplied upstream pin, so the supplied audit branch does not remove this stale claim."
  },
  {
    "topic": "Generic personal skills were not content-audited upstream",
    "evidence": "Upstream coverage.md:522 and 524 list banana-skill and rct-lite as 'External personal target; not audited'. A14.md:2786 states those aliases were inspected only with lstat/readlink and their personal targets were not followed.",
    "consequence": "This supplement actually read both SKILL.md documents and checked their integration references. It does not claim their contents were previously reviewed upstream."
  },
  {
    "topic": "No local alias failure is proven",
    "evidence": "All three tracked mode-120000 aliases resolve on this host; all three SKILL.md files and all twelve directly required resources are readable. The repository-local .codex directory is absent, not unreadable.",
    "consequence": "Personal absolute symlink targets remain a host-specific portability caveat, not a concrete current broken integration. No speculative portability finding is counted."
  },
  {
    "topic": "External ownership constrains fixes",
    "evidence": "SKILL-001 resolves to /Users/luka/src/meerkat/.claude/skills/meerkat-architecture/references/gotchas.md, outside the MobKit checkout.",
    "consequence": "No external target or repository file was changed. This finding needs independent adjudication and an explicitly authorized upstream-owned correction; it should not be silently marked fixed by the local MobKit documentation patch."
  }
]
```

## Supplemental prior-work coverage

The user's prior MobKit correction commit `01435e7ccda5e925fc2cf9f462327bdae9584d07` is not an ancestor of this baseline. An additional auditor compared its full 12-document diff against the current implementation and the first 140 findings. Nine nonduplicate remaining defects were added as K-001 through K-009 and independently confirmed.
